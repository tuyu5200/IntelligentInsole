<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
    <declare-styleable name="DrawPressurePath">
        <attr name="backgroundsrc" format="integer" />
        <attr name="lineweight" format="dimension" />
        <attr name="antialias" format="boolean" />
        <attr name="draw_color" format="color" />
        <attr name="isflip_horizontal" format="boolean" />
        <attr name="drawmode">
            <enum name="draw_line" value="1" />
            <enum name="draw_point" value="0" />
        </attr>
    </declare-styleable>
</resources>
